# Invocation Package

This package provides internal invocation tracking and management functionality.

## REST APIs

This package does not expose direct REST APIs. It provides internal functionality for tracking and managing invocations across other packages (client, registry, jobs, etc.).
