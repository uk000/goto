# Global Package

This package provides global configuration, flags, and shared state management.

## REST APIs

This package does not expose REST APIs. It provides internal global configuration and state management used by other packages.
